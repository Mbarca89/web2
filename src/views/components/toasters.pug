if errorMessage
  script.
    showToast({
      message: !{JSON.stringify(errorMessage)},
      type: "error",
      duration: 4000
    })

if successMessage
  script.
    showToast({
      message: !{JSON.stringify(successMessage)},
      type: "success"
    })